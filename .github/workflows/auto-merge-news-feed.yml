name: Auto-merge AI news feed PRs

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]
  workflow_dispatch:

permissions:
  actions: write
  pull-requests: write
  contents: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    if: github.event.pull_request.title == 'Update AI news feed'
    steps:
      - name: Close duplicate PRs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CURRENT_PR: ${{ github.event.pull_request.number }}
          GH_REPO: ${{ github.repository }}
        run: |
          gh pr list \
            --repo "$GH_REPO" \
            --state open \
            --search "Update AI news feed in:title" \
            --json number \
            --jq '.[].number' \
          | while read -r number; do
              if [ "$number" != "$CURRENT_PR" ]; then
                echo "Closing duplicate PR #$number"
                gh pr close "$number" --repo "$GH_REPO"
              fi
            done

      - name: Merge PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          CURRENT_PR: ${{ github.event.pull_request.number }}
        run: |
          gh pr merge "$CURRENT_PR" \
            --repo "$GH_REPO" \
            --merge

      - name: Trigger deploy
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: gh workflow run deploy.yml --repo "$GH_REPO" --ref main
